= provide(:title, 'All users')

%h1 Users list

= will_paginate

- @users.each do |user|
	.row.users
		.col-md-3.text-top
			= gravatar_for user
			= user.login
		.col-md-3.text-middle
			= user.email
		.col-md-2
			= link_to 'delete', 'delete_path'

= will_paginate